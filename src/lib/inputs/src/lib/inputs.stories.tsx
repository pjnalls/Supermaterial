import type { Meta } from '@storybook/react';
import { Inputs } from './inputs';

const Story: Meta<typeof Inputs> = {
  component: Inputs,
  title: 'Inputs',
};
export default Story;

export const Primary = {
  args: {},
};
