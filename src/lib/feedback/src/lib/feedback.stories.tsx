import type { Meta } from '@storybook/react';
import { Feedback } from './feedback';

const Story: Meta<typeof Feedback> = {
  component: Feedback,
  title: 'Feedback',
};
export default Story;

export const Primary = {
  args: {},
};
