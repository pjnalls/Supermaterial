import type { Meta } from '@storybook/react';
import { Navigation } from './navigation';

const Story: Meta<typeof Navigation> = {
  component: Navigation,
  title: 'Navigation',
};
export default Story;

export const Primary = {
  args: {},
};
