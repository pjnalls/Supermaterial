import type { Meta } from '@storybook/react';
import { DataDisplay } from './data-display';

const Story: Meta<typeof DataDisplay> = {
  component: DataDisplay,
  title: 'DataDisplay',
};
export default Story;

export const Primary = {
  args: {},
};
